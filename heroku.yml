setup:
  config:
    MUSESCORE_PATH: /usr/bin/mscore
    PDF_TO_MUSIC_PATH: /app/pdftomusicpro-1.7.6d.0/InstallFiles/usr/bin/'PDFtoMusic Pro'
    HEADLESS_MODE_ENABLED: True
build:
  docker:
    web: Dockerfile
